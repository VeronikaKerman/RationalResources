// Leftover

@PART[rr_boxblue_flat,rr_boxred_flat]:NEEDS[Kerbalism] 
{
	// RRConverter = Set // don't apply this as the loadout is entirely different and should not overlap
	// TODO: Freezer/Heaters
	RRPower = 0.25 // nerfed with consideration for slot count upgrade
	RRSlots = 1
}
